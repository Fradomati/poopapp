{
    "scripts": {
        "build": "cd ./client && npm install && npm run build",
        "postbuild": "copy \"./client/build/**/*\" \"./back/public\"",
        "heroku-postbuild": "npm run install:client && npm run build",
        "start": "npm run build && npm run node back/bin/www"
    }
}